class SpacewarPlayer {
    constructor(controls, pos, v) {
        this.ship = new SpacewarShip(pos, v);

        this.controls = controls;
    }

    keyUp(k) {

    }

    keyDown(k) {
        
    }
}